---
/**
 * feelings-project: Constanceライクな1ページ構成の最小サンプル
 * - フルスクロール
 * - セクションごとに色と余白を変える
 * - 右下にスクロール連動のランナー
 */
---

<html lang="ja">
    <head>
        <meta charset="utf-8" />
        <title>feelings-project</title>
        <meta name="viewport" content="width=device-width, initial-scale=1" />
    </head>

    <body>
        <header class="fp-header">
            <div class="fp-logo">feelings💙 SQL</div>
            <nav class="fp-nav">
                <a href="#story">Story</a>
                <a href="#cast">Cast</a>
                <a href="#learning">Learning</a>
                <a href="#news">News</a>
            </nav>
        </header>

        <main class="fp-main">
            {/* HERO / MOVIE */}
            <section class="fp-section fp-hero js-fade">
                <div class="fp-hero-inner">
                    <p class="fp-kicker">
                        A conversation between feelings and logic.
                    </p>
                    <h1 class="fp-title">
                        感情💙SQL<br />
                        <span
                            >動き出したくなる、思想をもった実務家のために。</span
                        >
                    </h1>
                    <p class="fp-lead">
                        アルカ（感情）とネミ（ロジック）の掛け合いを通じて、
                        データ社会に「人の温度」と「考える楽しさ」を取り戻すプロジェクト。
                    </p>

                    <a href="#story" class="fp-cta">物語からはじめる</a>
                </div>
            </section>

            {/* STORY */}
            <section id="story" class="fp-section fp-section-alt js-fade">
                <div class="fp-section-inner">
                    <h2>Story</h2>
                    <p>
                        夜更けのオフィス。クエリログの山に埋もれながら、
                        ネミはふと「なんで、こんなにしんどいんだろう」と手を止める。
                    </p>
                    <p>
                        そのとき、画面の奥からひょいっと顔を出したのが、
                        感情を司るAIの女の子・アルカだった――。
                    </p>
                </div>
            </section>

            {/* CAST / CHARACTERS */}
            <section id="cast" class="fp-section js-fade">
                <div class="fp-section-inner">
                    <h2>Cast</h2>
                    <div class="fp-cards">
                        <article class="fp-card">
                            <h3>アルカ</h3>
                            <p>
                                感情とノリを司るAIの女の子。考える前に動くタイプ。
                                データの“ざわざわ”を言語化するのが得意。
                            </p>
                        </article>

                        <article class="fp-card">
                            <h3>ネミ</h3>
                            <p>
                                温度のある理屈屋なエンジニア。ロジックで世界を整理しながらも、
                                どこか優しいツッコミ役。
                            </p>
                        </article>
                    </div>
                </div>
            </section>

            {/* LEARNING / GAMEPLAY に相当 */}
            <section id="learning" class="fp-section fp-section-alt js-fade">
                <div class="fp-section-inner">
                    <h2>What you’ll learn</h2>
                    <ul class="fp-list">
                        <li>感情から問いを立てるSQLの考え方</li>
                        <li>日常のモヤモヤをデータの問いに翻訳する技術</li>
                        <li>ROLLBACKしたくなる黒歴史との付き合い方</li>
                    </ul>
                </div>
            </section>

            {/* NEWS */}
            <section id="news" class="fp-section js-fade">
                <div class="fp-section-inner">
                    <h2>News</h2>
                    <ul class="fp-news">
                        <li>
                            <time datetime="2025-11-15">2025.11.15</time>
                            <span>第1話「問いから始まるSQL」準備中。</span>
                        </li>
                        <li>
                            <time datetime="2025-11-20">2025.11.20</time>
                            <span
                                >アルカ＆ネミのビジュアル、ティザー公開予定。</span
                            >
                        </li>
                    </ul>
                </div>
            </section>
        </main>

        {/* 右下ランナー（Constanceの走る主人公ポジション） */}
        <div id="runner" class="fp-runner">
            <div class="fp-runner-body">
                <span class="fp-runner-label">Arca &amp; Nemi</span>
                <div class="fp-runner-bar">
                    <div class="fp-runner-progress"></div>
                </div>
            </div>
        </div>

        <script is:inline>
            if (typeof window !== "undefined") {
                const fadeEls = document.querySelectorAll(".js-fade");
                const runner = document.getElementById("runner");
                const runnerProgress = runner
                    ? runner.querySelector(".fp-runner-progress")
                    : null;

                const io = new IntersectionObserver(
                    (entries) => {
                        entries.forEach((entry) => {
                            if (entry.isIntersecting) {
                                entry.target.classList.add("is-visible");
                                io.unobserve(entry.target);
                            }
                        });
                    },
                    { threshold: 0.15 },
                );

                fadeEls.forEach((el) => io.observe(el));

                const updateRunner = () => {
                    if (!runner || !runnerProgress) return;
                    const scrollTop = window.scrollY;
                    const doc = document.documentElement;
                    const scrollHeight = doc.scrollHeight - window.innerHeight;
                    const ratio =
                        scrollHeight > 0 ? scrollTop / scrollHeight : 0;

                    // 横に少しスライドさせる
                    const maxTranslate = 40; // px
                    const tx = ratio * maxTranslate;
                    runner.style.transform = `translateY(0) translateX(${tx}px)`;

                    // プログレスバー
                    runnerProgress.style.transform = `scaleX(${Math.max(
                        0.05,
                        Math.min(1, ratio),
                    )})`;
                };

                window.addEventListener("scroll", updateRunner);
                window.addEventListener("resize", updateRunner);
                updateRunner();
            }
        </script>

        <style>
            :root {
                --fp-bg: #040613;
                --fp-bg-alt: #090b1c;
                --fp-accent: #a5b4fc;
                --fp-accent-soft: #4f46e5;
                --fp-text: #f9fafb;
                --fp-muted: #9ca3af;
            }

            * {
                box-sizing: border-box;
            }

            body {
                margin: 0;
                font-family:
                    system-ui,
                    -apple-system,
                    BlinkMacSystemFont,
                    "Segoe UI",
                    sans-serif;
                background: radial-gradient(
                    circle at 0 0,
                    #0b1025,
                    #02030a 60%
                );
                color: var(--fp-text);
            }

            .fp-main {
                max-width: 880px;
                margin: 0 auto;
                padding: 72px 16px 96px;
            }

            .fp-header {
                position: sticky;
                top: 0;
                z-index: 20;
                display: flex;
                justify-content: space-between;
                align-items: center;
                padding: 10px 14px;
                backdrop-filter: blur(16px);
                background: linear-gradient(
                    to bottom,
                    rgba(3, 7, 18, 0.9),
                    rgba(3, 7, 18, 0.75),
                    transparent
                );
                border-bottom: 1px solid rgba(148, 163, 184, 0.3);
            }

            .fp-logo {
                font-size: 0.9rem;
                letter-spacing: 0.08em;
                text-transform: uppercase;
            }

            .fp-nav {
                display: flex;
                gap: 1rem;
                font-size: 0.8rem;
            }

            .fp-nav a {
                text-decoration: none;
                color: var(--fp-muted);
                position: relative;
            }

            .fp-nav a::after {
                content: "";
                position: absolute;
                left: 0;
                bottom: -2px;
                width: 100%;
                height: 1px;
                transform: scaleX(0);
                transform-origin: left;
                background: var(--fp-accent);
                transition: transform 160ms ease-out;
            }

            .fp-nav a:hover::after {
                transform: scaleX(1);
            }

            .fp-section {
                padding: 80px 0 64px;
            }

            .fp-section-alt {
                background: linear-gradient(
                    to bottom,
                    rgba(15, 23, 42, 0.5),
                    rgba(15, 23, 42, 0.1)
                );
                border-radius: 24px;
                margin-block: 24px;
                box-shadow: 0 18px 40px rgba(15, 23, 42, 0.8);
            }

            .fp-section-inner {
                padding: 0 4px;
            }

            h1,
            h2,
            h3 {
                margin: 0 0 0.75rem;
            }

            h2 {
                font-size: 1.35rem;
            }

            h3 {
                font-size: 1.05rem;
            }

            p {
                font-size: 0.95rem;
                line-height: 1.8;
                margin: 0.8rem 0;
                color: var(--fp-muted);
            }

            .fp-hero {
                padding-top: 96px;
            }

            .fp-hero-inner {
                padding-top: 32px;
                padding-bottom: 32px;
            }

            .fp-kicker {
                font-size: 0.8rem;
                letter-spacing: 0.14em;
                text-transform: uppercase;
                color: var(--fp-accent);
            }

            .fp-title {
                font-size: 2rem;
                line-height: 1.2;
                margin-top: 0.75rem;
            }

            .fp-title span {
                display: block;
                font-size: 0.9rem;
                font-weight: 400;
                color: var(--fp-muted);
                margin-top: 0.3rem;
            }

            .fp-lead {
                margin-top: 1.4rem;
            }

            .fp-cta {
                display: inline-flex;
                margin-top: 1.8rem;
                padding: 0.65rem 1.3rem;
                border-radius: 999px;
                background: radial-gradient(
                        circle at 0 0,
                        rgba(129, 140, 248, 0.26),
                        transparent 55%
                    ),
                    var(--fp-accent-soft);
                color: #e5e7eb;
                font-size: 0.9rem;
                text-decoration: none;
                border: 1px solid rgba(191, 219, 254, 0.7);
                box-shadow: 0 12px 30px rgba(15, 23, 42, 0.9);
                transition:
                    transform 150ms ease-out,
                    box-shadow 150ms ease-out;
            }

            .fp-cta:hover {
                transform: translateY(-2px);
                box-shadow: 0 18px 40px rgba(15, 23, 42, 1);
            }

            .fp-cards {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
                gap: 1rem;
                margin-top: 1.4rem;
            }

            .fp-card {
                padding: 0.9rem 1rem;
                border-radius: 18px;
                border: 1px solid rgba(148, 163, 184, 0.3);
                background: radial-gradient(
                        circle at 0 0,
                        rgba(129, 140, 248, 0.16),
                        transparent 55%
                    ),
                    rgba(15, 23, 42, 0.96);
                box-shadow: 0 12px 26px rgba(15, 23, 42, 0.9);
            }

            .fp-list {
                list-style: none;
                padding-left: 0;
                margin: 1rem 0 0;
            }

            .fp-list li::before {
                content: "✶";
                margin-right: 0.4rem;
                color: var(--fp-accent);
            }

            .fp-news {
                list-style: none;
                padding-left: 0;
                margin-top: 1rem;
                font-size: 0.9rem;
            }

            .fp-news li {
                display: flex;
                flex-wrap: wrap;
                gap: 0.6rem;
                margin-bottom: 0.5rem;
            }

            .fp-news time {
                font-feature-settings: "palt";
                color: var(--fp-muted);
                min-width: 6rem;
            }

            /* scroll fade-in */
            .js-fade {
                opacity: 0;
                transform: translateY(14px);
                transition:
                    opacity 420ms ease-out,
                    transform 420ms ease-out;
            }

            .js-fade.is-visible {
                opacity: 1;
                transform: translateY(0);
            }

            /* 右下ランナー */
            .fp-runner {
                position: fixed;
                right: 12px;
                bottom: 12px;
                z-index: 30;
                transition: transform 200ms ease-out;
            }

            .fp-runner-body {
                padding: 0.45rem 0.7rem;
                border-radius: 999px;
                border: 1px solid rgba(148, 163, 184, 0.7);
                background: radial-gradient(
                        circle at 0 0,
                        rgba(129, 140, 248, 0.26),
                        transparent 60%
                    ),
                    rgba(3, 7, 18, 0.96);
                box-shadow: 0 12px 30px rgba(15, 23, 42, 0.9);
                min-width: 160px;
            }

            .fp-runner-label {
                font-size: 0.72rem;
                text-transform: uppercase;
                letter-spacing: 0.14em;
                color: #e5e7eb;
            }

            .fp-runner-bar {
                margin-top: 0.25rem;
                width: 100%;
                height: 4px;
                border-radius: 999px;
                background: rgba(30, 64, 175, 0.7);
                overflow: hidden;
            }

            .fp-runner-progress {
                height: 100%;
                width: 100%;
                transform-origin: left;
                transform: scaleX(0.1);
                background: linear-gradient(to right, #a5b4fc, #f472b6);
                transition: transform 160ms ease-out;
            }

            @media (max-width: 720px) {
                .fp-main {
                    padding-top: 72px;
                }

                .fp-header {
                    padding-inline: 10px;
                }

                .fp-nav {
                    gap: 0.7rem;
                    font-size: 0.7rem;
                }

                .fp-title {
                    font-size: 1.6rem;
                }

                .fp-section {
                    padding: 64px 0 48px;
                }

                .fp-section-alt {
                    margin-inline: -4px;
                    border-radius: 18px;
                }

                .fp-runner-body {
                    min-width: 150px;
                }
            }
        </style>
    </body>
</html>
