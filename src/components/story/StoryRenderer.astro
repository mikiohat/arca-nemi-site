---
import ConversationBubble from "@talk/ConversationBubble.astro";
import type { LedgerUnit } from "@/content/ledger/sql";
import type { StoryLine } from "@/content/scripts/types";

type Props = {
  ledger: LedgerUnit;
  lines: StoryLine[];
};

const { ledger, lines } = Astro.props as Props;
---

<h1 class="fp-title">{ledger.title}</h1>
{
  ledger.story?.subtitle && (
    <p class="fp-subtitle">{ledger.story.subtitle}</p>
  )
}

{
  lines.map((l) => (
    <ConversationBubble speaker={l.speaker} emotion={l.emotion} peek={l.peek}>
      {l.text}
    </ConversationBubble>
  ))
}
